# ROS package overrides: Humble runs Python 3.10; house style is single quotes
# + %-formatting (deliberate — no UP/pyupgrade rules anywhere, see ADR-0013).
extend = "../pyproject.toml"
target-version = "py310"

[lint.flake8-quotes]
inline-quotes = "single"
