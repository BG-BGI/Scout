<?xml version="1.0" encoding="UTF-8" ?>
<!-- Fast DDS SUPER_CLIENT profile for DIAGNOSTIC SHELLS ONLY.

The stack discovers via the compose `discovery` service (Fast DDS Discovery
Server v2, 127.0.0.1:11811). v2 filters discovery data by topic, so a plain
client (any throwaway `docker compose run` shell, the ros2 daemon) sees only
the topics it has endpoints on - `ros2 topic list` / `node list` come back
near-empty. A SUPER_CLIENT receives the full graph from the server.

Usage inside any container (path is the bind mount):
  export FASTRTPS_DEFAULT_PROFILES_FILE=/ros_ws/src/scout/config/super_client.xml
  ros2 daemon stop && ros2 daemon start   # daemon must be reborn as super client
  ros2 topic list                          # full graph, no 16 s false negatives

The RemoteServer prefix encodes server-id 0 (see the Humble Discovery Server
tutorial); it must match `fastdds discovery -i 0` in docker-compose.yaml. -->
<dds>
    <profiles xmlns="http://www.eprosima.com/XMLSchemas/fastRTPS_Profiles">
        <participant profile_name="super_client_profile" is_default_profile="true">
            <rtps>
                <builtin>
                    <discovery_config>
                        <discoveryProtocol>SUPER_CLIENT</discoveryProtocol>
                        <discoveryServersList>
                            <RemoteServer prefix="44.53.00.5f.45.50.52.4f.53.49.4d.41">
                                <metatrafficUnicastLocatorList>
                                    <locator>
                                        <udpv4>
                                            <address>127.0.0.1</address>
                                            <port>11811</port>
                                        </udpv4>
                                    </locator>
                                </metatrafficUnicastLocatorList>
                            </RemoteServer>
                        </discoveryServersList>
                    </discovery_config>
                </builtin>
            </rtps>
        </participant>
    </profiles>
</dds>
